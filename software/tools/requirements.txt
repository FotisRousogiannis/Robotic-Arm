# Bench-testing tool dependencies (Raspberry Pi, Ubuntu Server).
# Install inside your venv:  pip install -r requirements.txt
#
# Not needed on a PC for SIMULATION mode — servo_test.py runs without
# these and just prints the pulses it would send.
adafruit-circuitpython-pca9685>=3.4.0
adafruit-blinka>=8.0.0
